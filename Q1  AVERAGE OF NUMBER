#include <iostream>
using namespace std;

int main() {
    float a, b, c;
    cout << "Enter three marks: ";
    cin >> a >> b >> c;

    float avg = (a + b + c) / 3;
    cout << "Average = " << avg;
    return 0;
}

